Raidlands vehicle token failed drop-spawn fallback - 2026-07-08

What changed:
- RaidlandsVehicleTokens is now v1.0.6.
- Failed dropped-token redemption no longer creates a refund token and removes the original dropped world item.
- If a dropped token cannot spawn its vehicle, the original dropped stack stays where the player threw it and can be picked up normally.
- Successful dropped-token spawns still consume exactly one token from the dropped stack.
- Direct `/raidvehicle` redemption still consumes from inventory only after a successful spawn.

Rust server files to upload:
- oxide/plugins/RaidlandsVehicleTokens.cs

Reload after upload:
- oxide.reload RaidlandsVehicleTokens

Quick live smoke:
- raidlands.vehicle.give <playerNameOrSteamId> rhib 1
- Drop the RHIB token on land.
- Expected: water-vehicle spawn check fails, the original dropped RHIB token remains in the world, and you can pick it back up.
- Drop a minicopter token while facing a clear area.
- Expected: minicopter spawns 5-15m ahead, nose points left, and the dropped stack decrements by one.

Detailed vehicle-token handoff:
- UPDATED_FILES_FOR_UPLOAD_VEHICLE_TOKENS_CID_DROP_2026-07-08.md

Local verification:
- Roslyn compile passed for oxide/plugins/RaidlandsVehicleTokens.cs v1.0.6.
- Remaining warnings were expected Oxide injected-plugin-reference/config-field warnings.

Local source hash:
- oxide/plugins/RaidlandsVehicleTokens.cs SHA256: 49E09CA8B40B59D3B5256D13F1F7A10F71F06BE16866333B31D520A254A256F9

Older upload notes follow.

---

Portable Airstrikes MoveItem crash and kinematic velocity repair - 2026-07-08

What changed:
- PortableAirstrikes is now v0.1.36.
- Added `ConfigVersion=31`.
- Airstrike CID binoculars are back to one physical item per slot with charges stored in `instanceData.dataInt`.
- `AirstrikeItem.MaxStackSize=1`; `MaxChargesPerItem=65535`.
- `StackSizeController.json` now pins `raidlands.airstrike.designator=1` and keeps `tool.binoculars=1`.
- Existing bad physical stacks normalize into one movable item named like `Airstrike Targeting Binoculars x25`.
- Give/API/kit/loot paths create charged single items instead of Rust inventory stacks.
- Visual flyover rigidbody handling no longer writes velocity/angular velocity after a body has been made kinematic.
- SpawnHeli's fetch/teleport path now skips velocity clearing when the heli rigidbody is kinematic.

Rust server files to upload:
- oxide/plugins/PortableAirstrikes.cs
- oxide/config/PortableAirstrikes.json
- oxide/config/StackSizeController.json
- oxide/plugins/SpawnHeli.cs

Runtime dependencies:
- Keep oxide/plugins/CustomItemDefinitions.cs uploaded.
- Keep oxide/plugins/StackSizeController.cs uploaded.
- Keep oxide/data/PortableAirstrikes/airstrike-targeting-binoculars.png uploaded.

Reload after upload:
- oxide.reload CustomItemDefinitions
- oxide.reload StackSizeController
- oxide.reload SpawnHeli
- oxide.reload PortableAirstrikes

Quick live smoke:
- Confirm the PortableAirstrikes reload banner reports v0.1.36.
- /strike debug item
- Expected: actualStack=1, maxCharges=65535, and the PNG-backed icon source.
- portableairstrikes.giveitem <playerNameOrSteamId> 25
- Expected: one physical item named Airstrike Targeting Binoculars x25, not a Rust inventory stack count.
- Move the item between inventory slots and containers.
- Expected: no disconnect, no RPC Error in MoveItem, and no split_Amount <= 0.
- Use one valid strike.
- Expected: the item remains one physical item and decrements to Airstrike Targeting Binoculars x24.
- Run one attack-heli/rocket-run visual strike.
- Expected: no repeated kinematic linear/angular velocity spam from the flyover visual.

Detailed Airstrikes handoff:
- UPDATED_FILES_FOR_UPLOAD_PORTABLE_AIRSTRIKES_MOVEITEM_KINEMATIC_2026-07-08.md

Local verification:
- JSON validation passed for oxide/config/PortableAirstrikes.json and oxide/config/StackSizeController.json.
- Roslyn compile passed for oxide/plugins/PortableAirstrikes.cs v0.1.36.
- Roslyn compile sanity check passed for oxide/plugins/SpawnHeli.cs.
- Remaining warnings were expected Unity obsolete API and Oxide/config field warnings.

Local source/config hashes:
- oxide/plugins/PortableAirstrikes.cs SHA256: 2A32C475774F5445655F4B7A61A084BFFE0FC7E7851F9C6FB65ACEE26CBD96C5
- oxide/config/PortableAirstrikes.json SHA256: 5B1396CBE1F627D0C5A765E247E4900E127A409C8A1A79968EADC1D805F1B5BC
- oxide/config/StackSizeController.json SHA256: 2CDC563B43F5FEF55B102A3BD98C5163F2E70FAD14F3299E8D9587EB94506796
- oxide/plugins/SpawnHeli.cs SHA256: 31F9557E2C5B8F739B75047AD67B811AADD9D62CBD5B3D3A95E72A98C8E4728B

Older upload notes follow.

---

Raidlands vehicle token forward spawn placement - 2026-07-08

What changed:
- RaidlandsVehicleTokens is now v1.0.6.
- Dropped-token and direct token redemption now request vehicle placement from the player's facing direction instead of the dropped item/player feet position.
- Failed dropped-token spawns leave the original dropped token stack in the world so it can be picked up normally.
- Default placement is random 5-15m in front of the player, with yaw offset -90 degrees so the vehicle front points toward the player's left.
- New config keys: Token Spawn Min Forward Distance, Token Spawn Max Forward Distance, Token Spawn Yaw Offset Degrees.
- Requested positions are ground-corrected before SpawnHeli/VehicleLicence receive them; SpawnHeli token spawns still use the existing Dropped Spawn Vertical Offset.
- Existing cooldown, safe-zone, mounted, nearby-player, water-vehicle, space, and token-only VehicleLicence checks remain in place.

Rust server files to upload:
- oxide/plugins/RaidlandsVehicleTokens.cs
- oxide/config/RaidlandsVehicleTokens.json
- oxide/plugins/RaidlandsVehicleTokens.json if you keep this staged config copy on the live server

Runtime dependencies:
- Keep the existing token-spawn hook versions of oxide/plugins/SpawnHeli.cs and oxide/plugins/VehicleLicence.cs uploaded from the CID/drop-spawn pass.
- Keep oxide/plugins/CustomItemDefinitions.cs loaded before RaidlandsVehicleTokens if the live server is using CID-backed vehicle token items.

Reload after upload:
- oxide.reload RaidlandsVehicleTokens

Full reload order if deploying the whole CID/drop-spawn vehicle-token bundle at the same time:
- oxide.reload CustomItemDefinitions
- oxide.reload SpawnHeli
- oxide.reload VehicleLicence
- oxide.reload RaidlandsVehicleTokens

Quick live smoke:
- raidlands.vehicle.give <playerNameOrSteamId> minicopter 5
- Drop the stack while facing a clear area.
- Expected: one minicopter spawns roughly 5-15m in front of you, its nose points to your left, and the dropped stack becomes 4.
- Drop an RHIB token on land.
- Expected: spawn fails and the original dropped RHIB token remains in the world for pickup.
- Run /raidvehicle minicopter while holding or carrying a minicopter token.
- Expected: the direct redemption path uses the same forward/left-facing placement and consumes one token only after a successful spawn.

Detailed vehicle-token handoff:
- UPDATED_FILES_FOR_UPLOAD_VEHICLE_TOKENS_CID_DROP_2026-07-08.md

Local verification:
- JSON validation passed for oxide/config/RaidlandsVehicleTokens.json and oxide/plugins/RaidlandsVehicleTokens.json.
- Roslyn compile passed for oxide/plugins/RaidlandsVehicleTokens.cs v1.0.6.
- Compile warnings were the expected Oxide injected-plugin-reference/config-field warnings.

Local source/config hashes:
- oxide/plugins/RaidlandsVehicleTokens.cs SHA256: 49E09CA8B40B59D3B5256D13F1F7A10F71F06BE16866333B31D520A254A256F9
- oxide/config/RaidlandsVehicleTokens.json SHA256: 2D0650CB5C61E491BF59DD3B219CA8338AE88CD6AB4B013BCF94C4EC28BA6AAA
- oxide/plugins/RaidlandsVehicleTokens.json SHA256: FF2F34E928556158E26A8D43D8AE676FA79F82A82D57E392D1BEC0D6013390D8

Older upload notes follow.

---

Portable Airstrikes patrol-heli spawn-order fix - 2026-07-08

What changed:
- PortableAirstrikes is now v0.1.35.
- Fixed the patrol-heli visual spawn order for `attack_heli`/rocket-run delivery visuals.
- The native patrol-heli prefab now keeps its prefab GUID references intact until after `Spawn()`, avoiding the live `GUIDToPath: guid is empty` lead-in and `spawn patrol helicopter failed: Object reference not set to an instance of an object` warning.
- The spawned patrol-heli visual still disables native brain/AI and clears optional gib/fireball/map-marker references after spawn so the carrier can follow the scripted rocket-run path.
- `ConfigVersion` remains `30`; this pass has no config migration.
- Existing cargo-plane, F-15, drone, mortar, payload, RP/token/cooldown, warning marker, audit history, homing gate, and destroyable-carrier behavior were left unchanged.

Rust server files to upload:
- oxide/plugins/PortableAirstrikes.cs
- oxide/config/PortableAirstrikes.json only if the v0.1.32+ vehicle-alignment config has not already been uploaded

Local source/config hashes:
- oxide/plugins/PortableAirstrikes.cs SHA256: 14C3E77CA32A07722DD1786A0BB4A9A2D68697F177FCC7CBA664032230199E5E
- oxide/config/PortableAirstrikes.json SHA256: 917F3170D61FC0EEF1809CD0F9F3133392D4B153ED1954091D5E7184F7CE669B

Runtime dependencies to keep uploaded:
- oxide/plugins/CustomItemDefinitions.cs
- oxide/plugins/StackSizeController.cs
- oxide/config/StackSizeController.json
- oxide/data/PortableAirstrikes/airstrike-targeting-binoculars.png

Detailed Airstrikes handoff:
- UPDATED_FILES_FOR_UPLOAD_PORTABLE_AIRSTRIKES_PATROL_HELI_SPAWN_ORDER_2026-07-08.md

Reload after upload:
- oxide.reload PortableAirstrikes

Quick live smoke:
- Confirm the PortableAirstrikes reload banner reports v0.1.35.
- `/strike debugping`, then `/strike rocket_run`; confirm a native patrol-heli approach before rockets release and no `GUIDToPath: guid is empty` or `visual rocket run could not spawn` warning.
- `/strike debugping`, then `/strike hv_rocket_run`; confirm the same patrol-heli approach.
- `/strike debugping`, then `/strike incendiary_rocket_run`; confirm the same patrol-heli approach.
- Check `/strike debug history 10` and `/strike debug stats`.
- If the patrol-heli visual still does not appear, turn on `General.DebugMode=true`, reload, repeat `/strike rocket_run`, and capture the new phase-specific warning.

Older upload notes follow.

---

Portable Airstrikes stackable CID binoculars - 2026-07-08

What changed:
- PortableAirstrikes is now v0.1.30.
- Added `ConfigVersion=27`.
- Airstrike binocular counts are physical Rust stack amounts again, so inventory shows the real stack count overlay.
- Old charge-backed `instanceData.dataInt` counts migrate into the physical item stack amount during inventory normalization.
- `AirstrikeItem.MaxStackSize=65535` and `MaxChargesPerItem=65535`.
- StackSizeController config now sets `raidlands.airstrike.designator=65535` while leaving vanilla `tool.binoculars=1`.
- StackSizeController now applies configured stack sizes when CID registers custom item definitions after StackSizeController has already initialized.
- Existing targeting, icon, warnings, RP/cooldowns, webhooks, and strike payload behavior were left unchanged.

Rust server files to upload:
- oxide/plugins/PortableAirstrikes.cs
- oxide/plugins/StackSizeController.cs
- oxide/config/PortableAirstrikes.json
- oxide/config/StackSizeController.json

Runtime dependencies to keep uploaded:
- oxide/plugins/CustomItemDefinitions.cs
- oxide/data/PortableAirstrikes/airstrike-targeting-binoculars.png

Detailed Airstrikes handoff:
- UPDATED_FILES_FOR_UPLOAD_PORTABLE_AIRSTRIKES_STACKABLE_CID_2026-07-08.md

Reload after upload:
- oxide.reload CustomItemDefinitions
- oxide.reload StackSizeController
- oxide.reload PortableAirstrikes

Quick live smoke:
- Confirm the PortableAirstrikes reload banner reports v0.1.30.
- Run `/strike debug item`; expected `actualStack=65535`, `maxCharges=65535`, and PNG-backed icon source.
- Run `portableairstrikes.giveitem <playerNameOrSteamId> 25`; expected one inventory slot with the custom airstrike binocular icon and Rust stack count `x25`.
- Move the stack between inventory slots/containers; expected no disconnect and no stuck held viewmodel.
- Use one valid strike; expected the physical stack count decrements by one.

Previous upload note follows.

---

Portable Airstrikes binocular stack/icon and rotor-wash client error fix - 2026-07-08

What changed:
- PortableAirstrikes is now v0.1.29.
- Added `ConfigVersion=26`.
- Restored charge-backed binocular behavior: `MaxChargesPerItem` defaults/clamps to `65535`.
- Old physical binocular stacks such as inventory `x25` normalize into one physical item with stored charges in `instanceData.dataInt`.
- If the repaired stacked binocular was actively held, PortableAirstrikes clears the active held item so the client viewmodel can refresh.
- Existing CID airstrike definitions are refreshed on reload so the current PNG icon and stack metadata are rebuilt.
- Removed direct rotor-wash effect sends because `rotorwashparticles_small` and `rotorwashparticles_large` live in non-autoloaded `AssetScene-props.other`.
- Existing payloads, RP/cooldowns, warnings, webhooks, and strike damage were left unchanged.

Rust server plugin file to upload:
- oxide/plugins/PortableAirstrikes.cs

Runtime dependencies to keep uploaded:
- oxide/plugins/CustomItemDefinitions.cs
- oxide/data/PortableAirstrikes/airstrike-targeting-binoculars.png
- oxide/config/StackSizeController.json

Local source hash:
- SHA256: 4F9ECC49167F4FCCA54FFC6EFC376D6ACB5C9BB330E1C02ABA34BA3565A45C78

Detailed Airstrikes handoff:
- UPDATED_FILES_FOR_UPLOAD_PORTABLE_AIRSTRIKES_BINOCULAR_STACK_ICON_ROTOR_2026-07-08.md

Reload after upload:
- oxide.reload CustomItemDefinitions
- oxide.reload PortableAirstrikes

Quick live smoke:
- Confirm the reload banner reports v0.1.29.
- Run `/strike debug item`; expected `actualStack=1`, `maxCharges=65535`, and `icon=<fileId> from ...airstrike-targeting-binoculars.png`.
- Existing inventory `x25` airstrike binocular stacks should become one physical item named like `Airstrike Targeting Binoculars x25`, without a Rust stack count overlay.
- Run `portableairstrikes.giveitem <playerNameOrSteamId> 25`; expected one physical item with 25 stored charges.
- Run a drone/aircraft visual strike; expected no new client overlay entries for `rotorwashparticles_small.prefab` or `rotorwashparticles_large.prefab`.

Previous upload note follows.

---

Portable Airstrikes client-safe visual prefab fix - 2026-07-08

What changed:
- PortableAirstrikes is now v0.1.28.
- Replaced the flyover air-movement cue `projectile-flight-large.prefab` with autoloaded `projectile-flight.prefab`.
- Stopped sending direct MLRS `pfx_mlrs_rocket_thrust.prefab` effects.
- This targets the client overlay errors requiring `AssetScene-props.other` while airstrike payloads are falling.
- No config upload or migration is required; `ConfigVersion` remains 25.

Rust server plugin file to upload:
- oxide/plugins/PortableAirstrikes.cs

Local source hash:
- SHA256: BD32EA2D20B32799191ED6076B24AB45121F1E4DC9176B1F22F9FE1F37082884

Detailed Airstrikes handoff:
- UPDATED_FILES_FOR_UPLOAD_PORTABLE_AIRSTRIKES_VISUAL_PREFAB_SAFE_2026-07-08.md

Reload after upload:
- oxide.reload PortableAirstrikes

Quick live smoke:
- Confirm the reload banner reports v0.1.28.
- Run `/strike propane_bomb_drop`.
- Expected: payloads still deliver, with no new client overlay entries for `projectile-flight-large.prefab`.
- If `mini_mlrs` or `full_mlrs` is deliberately enabled, run one MLRS strike in a safe area.
- Expected: rockets still launch, with no new client overlay entries for `pfx_mlrs_rocket_thrust.prefab`.

Local verification:
- Confirmed the two removed noisy prefabs are in non-autoloaded `AssetScene-props.other` in `Bundles/AssetSceneManifest.json`.
- Roslyn compile check passed for oxide/plugins/PortableAirstrikes.cs v0.1.28 against RustDedicated_Data/Managed.

Previous upload note follows.

---

Raidlands RoamBots native scientist audio mute - 2026-07-08

What changed:
- RaidlandsRoamBots is now v0.3.71.
- Native legacy-scientist radio chatter and death chatter are muted during RoamBots body preparation.
- The fix clears `ScientistNPC.RadioChatterEffects` and `DeathEffects`, cancels any queued `PlayRadioChatter`, and leaves RoamBots hearing, bot chat, weapon fire, tactical AI, killfeed, stats, and loot unchanged.
- No `oxide/config/RaidlandsRoamBots.json` upload is required for this specific fix unless you also want to deploy other staged config changes already present in the working tree.

Rust server plugin file to upload:
- oxide/plugins/RaidlandsRoamBots.cs

Detailed RoamBots handoff:
- UPDATED_FILES_FOR_UPLOAD_ROAMBOTS_SCIENTIST_AUDIO_2026-07-08.md

Reload after upload:
- oxide.reload RaidlandsRoamBots

Quick live smoke:
- raidbots.diag
- raidbots.enable 1
- Stand near the spawned bot, then damage/kill it.
- Expected: no native scientist radio/alert/death chatter; weapon fire and RoamBots behavior still work.

Local verification:
- `oxide/config/RaidlandsRoamBots.json` parsed successfully.
- Roslyn compile check passed for oxide/plugins/RaidlandsRoamBots.cs v0.3.71 against RustDedicated_Data/Managed with Oxide.References.dll and glTFast.Newtonsoft.dll excluded.
- Remaining warnings are expected Oxide plugin-reference/future-field warnings.

Previous upload note follows.

---

Portable Airstrikes charge-backed binoculars MoveItem fix - 2026-07-08

What changed:
- PortableAirstrikes is now v0.1.25.
- Added `ConfigVersion=25`.
- Actual airstrike binocular item stack size is back to `1`.
- Added `AirstrikeItem.MaxChargesPerItem`, default `65535`.
- Airstrike binocular charge count is now stored on the item in `instanceData.dataInt`.
- `portableairstrikes.giveitem <playerNameOrSteamId> 25` now creates one physical item named `Airstrike Targeting Binoculars x25` instead of a Rust inventory stack of 25.
- Successful strikes consume one stored charge; the item is removed when charges reach zero.
- Existing old airstrike stacks are normalized on plugin reload and player reconnect from `amount > 1` into `amount=1` plus stored charges.
- StackSizeController fallback sizes for `tool.binoculars` and `raidlands.airstrike.designator` are back to `1`.
- Existing automatic raycast targeting, scrollable picker, RP/cooldown behavior, warnings, webhooks, and strike executors were left unchanged.

Rust server plugin/config files to upload:
- oxide/plugins/PortableAirstrikes.cs
- oxide/config/StackSizeController.json

Runtime dependencies to keep from the v0.1.22 CID pass:
- oxide/plugins/CustomItemDefinitions.cs
- oxide/data/PortableAirstrikes/airstrike-targeting-binoculars.png

Detailed Airstrikes handoff:
- UPDATED_FILES_FOR_UPLOAD_PORTABLE_AIRSTRIKES_CHARGE_BACKED_BINOCULARS_2026-07-08.md

Reload after upload:
- oxide.reload CustomItemDefinitions
- oxide.reload StackSizeController
- oxide.reload PortableAirstrikes

Immediate live repair smoke:
- /strike debug item
- Expected: `actualStack=1` and `maxCharges=65535`.
- portableairstrikes.giveitem <playerNameOrSteamId> 25
- Expected: one physical item appears, named `Airstrike Targeting Binoculars x25`.
- Move the item between inventory slots and containers.
- Expected: no disconnect, no `split_Amount <= 0`, and the item remains movable.
- Confirm one valid strike consumes one charge and updates the item name/count.

Local verification:
- Roslyn compile check passed for oxide/plugins/PortableAirstrikes.cs against RustDedicated_Data/Managed with Oxide.References.dll and glTFast.Newtonsoft.dll excluded.
- JSON parse check passed for oxide/config/StackSizeController.json; both airstrike stack entries report actual stack size `1`.
- Direct trailing-whitespace scan passed for changed plugin/config/docs/upload files.

Previous upload note follows.

---

Portable Airstrikes targeting binoculars and default strike flow - 2026-07-08

What changed:
- PortableAirstrikes is now v0.1.21.
- Added `ConfigVersion=21`.
- Changed the default configured item to `tool.binoculars` named `Airstrike Targeting Binoculars`.
- Added persisted player defaults in `PortableAirstrikes_Data.DefaultStrikeByUser`.
- Added `/strike default show`, `/strike default <strikeId>`, and `/strike default clear`.
- Pinging while holding the configured item now creates a short-lived cyan/amber airstrike target marker.
- If the player has no saved default, that tool ping opens `/strike`; the confirmed selection is saved as their default.
- If the player has a saved default, that tool ping attempts the default strike immediately.
- Added `OnRaidlandsCreateKitItem(...)` support so Kits can create the configured airstrike item through PortableAirstrikes' own item creator.
- Generated `Docs/AirStrikes/assets/airstrike-targeting-binoculars.png` as a project reference icon.

Rust server plugin file to upload:
- oxide/plugins/PortableAirstrikes.cs

Optional website/docs asset:
- Docs/AirStrikes/assets/airstrike-targeting-binoculars.png

Detailed Airstrikes handoff:
- UPDATED_FILES_FOR_UPLOAD_PORTABLE_AIRSTRIKES_BINOCULARS_2026-07-08.md

Reload after upload:
- oxide.reload PortableAirstrikes

Binocular/default smoke:
- portableairstrikes.giveitem <playerNameOrSteamId> 2
- Equip `Airstrike Targeting Binoculars`.
- Place a ping while holding the item.
- Expected with no saved default: a cyan/amber target marker appears and `/strike` opens.
- Confirm a valid strike; it should be saved as the player's default.
- Place a second ping while holding the item.
- Expected with a saved default: the default strike is attempted immediately.

Local verification:
- Confirmed `tool.binoculars` exists locally, is holdable/usable, and reports `HasSkins=false`.
- Roslyn compile check passed for oxide/plugins/PortableAirstrikes.cs against RustDedicated_Data/Managed with Oxide.References.dll and glTFast.Newtonsoft.dll excluded.
- Direct trailing-whitespace scan passed for oxide/plugins/PortableAirstrikes.cs, Docs/AirStrikes/portable_airstrikes_development_log.md, Docs/AirStrikes/portable_airstrikes_options_tables_v2.md, and UPDATED_FILES_FOR_UPLOAD_PORTABLE_AIRSTRIKES_BINOCULARS_2026-07-08.md.

Previous upload note follows.

---

Portable Airstrikes high-rate visual movement and repeated flyover sound cues - 2026-07-08

What changed:
- PortableAirstrikes is now v0.1.20.
- Added `ConfigVersion=20`.
- Added `DeliveryVisuals.SpawnRotorWashEffects`, default `true`.
- Added `DeliveryVisuals.FlyoverSoundIntervalSeconds`, default `0.75`.
- Changed the default `DeliveryVisuals.VisualMoveIntervalSeconds` from `0.1` to `0.04`; old v0.1.18/v0.1.19 configs still at `0.2` or `0.1` migrate to `0.04` on reload.
- Scripted flyover movement now uses immediate network updates at the tighter interval instead of queued `0.1`-second updates.
- Visual vehicle entities set their local `On` flag, and player-helicopter visuals attempt to keep engine/fuel state active for better client-side animation/audio.
- Drone and aircraft flyovers now play repeated positional sound bursts along the flight path while enabled.
- Sound bursts now combine vehicle-engine, projectile-flight, large fast-falloff, bullet-flyby, and optional rotor wash effects depending on the visual type.
- Existing direct/CUI validation, RP/token/cooldown/refund behavior, payload timing/damage, warning fanout, heavy warning markers, audit/webhook behavior, default-disabled homing/MLRS gates, loot behavior, and monument blocking behavior were left unchanged.

Rust server plugin file to upload:
- oxide/plugins/PortableAirstrikes.cs

Local source hash:
- SHA256: E0B6881A61357AA0A9D5E66FB1B39649B22D977075D6A30BDB11E9CE309567D1

Detailed Airstrikes handoff:
- UPDATED_FILES_FOR_UPLOAD_PORTABLE_AIRSTRIKES_VISUAL_HIGH_RATE_2026-07-08.md

Reload after upload:
- oxide.reload PortableAirstrikes

Safe visual smoke:
- /strike debugping
- /strike beancan_drop
- /strike debugping
- /strike a10_strafe
- /strike debugping
- /strike mortar_he
- /strike debug history 5
- /strike debug stats

Expected:
- Plugin banner reports v0.1.20.
- Config saves `ConfigVersion=20`.
- Existing default visual intervals migrate to `DeliveryVisuals.VisualMoveIntervalSeconds=0.04`.
- Drone and attack-heli/cargo-plane flyovers should look less stepped than v0.1.19.
- Drone and aircraft flyovers should have repeated positional sound cues while `DeliveryVisuals.SpawnFlyoverSoundEffects=true`.
- Drone and attack-heli flyovers can play rotor wash effects while `DeliveryVisuals.SpawnRotorWashEffects=true`.
- Visuals clean up after completion/failure/cancel/unload without killing native payload projectiles early.

Local verification:
- Confirmed v0.1.20 sound/effect prefabs exist in Bundles/AssetSceneManifest.json for projectile-flight-large, large-sound-fast-falloff, and large/small rotor wash effects.
- Roslyn compile check passed for oxide/plugins/PortableAirstrikes.cs against RustDedicated_Data/Managed with Oxide.References.dll and glTFast.Newtonsoft.dll excluded.
- Direct trailing-whitespace scan passed for oxide/plugins/PortableAirstrikes.cs, Docs/AirStrikes/portable_airstrikes_development_log.md, UPDATED_FILES_FOR_UPLOAD.txt, and UPDATED_FILES_FOR_UPLOAD_PORTABLE_AIRSTRIKES_VISUAL_HIGH_RATE_2026-07-08.md.

Previous upload note follows.

---

Portable Airstrikes visual smoothing, sound cues, and MLRS plane visibility - 2026-07-08

What changed:
- PortableAirstrikes is now v0.1.19.
- Added `ConfigVersion=19`.
- Added `DeliveryVisuals.SpawnFlyoverSoundEffects`, default `true`.
- Added `DeliveryVisuals.MlrsAircraftFlyoverHeight`, default `58`.
- Changed the default `DeliveryVisuals.VisualMoveIntervalSeconds` from `0.2` to `0.1`; old v0.1.18 configs still at `0.2` migrate to `0.1` on reload.
- Scripted flyover movement now uses queued network updates instead of immediate network updates on every movement tick.
- Drone and aircraft flyovers now play positional sound cues while enabled.
- Rocket-run projectiles now trigger a launch effect, and MLRS rockets now trigger MLRS backfire/thrust effects.
- MLRS aircraft visuals now use the same approach direction as the MLRS rocket salvo and a lower MLRS-specific flyover height.
- Existing direct/CUI validation, RP/token/cooldown/refund behavior, payload timing/damage, warning fanout, heavy warning markers, audit/webhook behavior, default-disabled homing/MLRS gates, loot behavior, and monument blocking behavior were left unchanged.

Rust server plugin file to upload:
- oxide/plugins/PortableAirstrikes.cs

Local source hash:
- SHA256: 87B5AFB7E1BC757EAD7D6A315EF29F1E8865535A58957E6C185474B784871D1F

Detailed Airstrikes handoff:
- UPDATED_FILES_FOR_UPLOAD_PORTABLE_AIRSTRIKES_VISUAL_SMOOTHING_2026-07-08.md

Reload after upload:
- oxide.reload PortableAirstrikes

Safe visual smoke:
- /strike debugping
- /strike beancan_drop
- /strike debugping
- /strike a10_strafe
- /strike debugping
- /strike mortar_he
- /strike debug history 5
- /strike debug stats

Expected:
- Plugin banner reports v0.1.19.
- Drone and attack-heli/cargo-plane flyovers should look smoother than v0.1.18.
- Drone and aircraft flyovers should have positional sound cues while `DeliveryVisuals.SpawnFlyoverSoundEffects=true`.
- MLRS should show a lower aircraft pass aligned with the same approach direction as the rocket salvo.
- Visuals clean up after completion/failure/cancel/unload without killing native payload projectiles early.

Local verification:
- Confirmed sound/effect prefabs exist in Bundles/AssetSceneManifest.json for drone deploy, dangerous vehicle engine, bullet flyby, rocket launch, MLRS backfire, and MLRS rocket thrust.
- Roslyn compile check passed for oxide/plugins/PortableAirstrikes.cs against RustDedicated_Data/Managed with Oxide.References.dll and glTFast.Newtonsoft.dll excluded.
- Direct trailing-whitespace scan passed for oxide/plugins/PortableAirstrikes.cs, Docs/AirStrikes/portable_airstrikes_development_log.md, UPDATED_FILES_FOR_UPLOAD.txt, and UPDATED_FILES_FOR_UPLOAD_PORTABLE_AIRSTRIKES_VISUAL_SMOOTHING_2026-07-08.md.

Previous upload note follows.

---

Portable Airstrikes warning fanout diagnostics - 2026-07-08

What changed:
- PortableAirstrikes is now v0.1.17.
- Added `ConfigVersion=17`.
- Added admin diagnostic command:
  - /strike debug warnings <strikeId>
- The warning debug command previews who would receive accepted-strike warnings for the caller's current stored target without sending messages.
- Accepted strike calls now use the same shared recipient-selection path as the preview command.
- Accepted strike audit messages now include warning fanout counts.
- Server console now prints a `Warning fanout:` line for each accepted strike with call ID, strike ID, caller, recipient counts, config mode, nearby eligibility, radius, and marker state.
- Stored stats now include `warning_fanout_calls`, `warning_team_recipients`, `warning_nearby_recipients`, and `warning_no_recipients`.
- Existing warning message text, direct/CUI validation, RP/token/cooldown/refund behavior, cancel/status, heavy warning markers, local audit history, default-disabled homing/MLRS gates, loot behavior, monument blocking behavior, and Discord webhook behavior were left unchanged.

Rust server plugin file to upload:
- oxide/plugins/PortableAirstrikes.cs

Local source hash:
- SHA256: 4D193DAE60A6D82F241F2E43B28A4AAD5929CF92AD41F08B70F0174186713D3B

Detailed Airstrikes handoff:
- UPDATED_FILES_FOR_UPLOAD_PORTABLE_AIRSTRIKES_WARNING_DEBUG_2026-07-08.md

Reload after upload:
- oxide.reload PortableAirstrikes

Solo diagnostic smoke:
- /strike debugping
- /strike debug warnings beancan_drop
- /strike beancan_drop
- /strike debug history 5
- /strike debug stats

Expected:
- The preview reports zero recipients if you are solo, or lists online team recipients if any exist.
- The accepted strike still works normally.
- Server console prints a `Warning fanout:` line for the accepted call.
- `/strike debug history 5` includes warning fanout counts in the started audit row.
- `/strike debug stats` can show the new warning counters after at least one accepted call.

Local verification:
- Roslyn compile check passed for oxide/plugins/PortableAirstrikes.cs against RustDedicated_Data/Managed with Oxide.References.dll and glTFast.Newtonsoft.dll excluded.
- Direct trailing-whitespace scan passed for oxide/plugins/PortableAirstrikes.cs, Docs/AirStrikes/portable_airstrikes_development_log.md, UPDATED_FILES_FOR_UPLOAD.txt, and UPDATED_FILES_FOR_UPLOAD_PORTABLE_AIRSTRIKES_WARNING_DEBUG_2026-07-08.md.

Previous upload note follows.

---

Portable Airstrikes in-game warning fanout - 2026-07-08

What changed:
- PortableAirstrikes is now v0.1.16.
- Added `ConfigVersion=16`.
- Added in-game warning config:
  - `General.NotifyCallerTeamOnAcceptedStrike`, default `true`
  - `General.NotifyNearbyPlayersOnHeavyStrikes`, default `false`
  - `General.NearbyHeavyStrikeWarningRadius`, default `120`
- Accepted strikes now notify online Rust teammates other than the caller when team warnings are enabled.
- Heavy strikes can optionally notify online nearby players inside the configured radius.
- Team and nearby warning recipients are deduplicated so a nearby teammate should only receive one warning message.
- Discord webhook behavior was left unchanged and still requires a real private webhook URL for live verification.
- Existing direct/CUI validation, RP/token/cooldown/refund behavior, cancel/status, heavy warning markers, local audit history, default-disabled homing/MLRS gates, loot behavior, and monument blocking behavior were left unchanged.

Rust server plugin file to upload:
- oxide/plugins/PortableAirstrikes.cs

Local source hash:
- SHA256: 24AF07B47E4D25F99F76CB221463629870460CDE4331A3999C95E29ED0B84536

Detailed Airstrikes handoff:
- UPDATED_FILES_FOR_UPLOAD_PORTABLE_AIRSTRIKES_WARNINGS_2026-07-08.md

Reload after upload:
- oxide.reload PortableAirstrikes

Safe default smoke:
- /strike debugping
- /strike
- /strike beancan_drop
- /strike debug history 5
- If two online team members are available, confirm the teammate receives one in-game warning.

Optional nearby heavy-warning smoke:
- Reload once so the new `General` keys are written to config.
- Set `General.NotifyNearbyPlayersOnHeavyStrikes=true`.
- Reload `PortableAirstrikes`, then run a heavy strike with another online player inside `General.NearbyHeavyStrikeWarningRadius`.
- Expected: the nearby player receives one warning message; nearby teammates should not receive duplicates.

Local verification:
- Roslyn compile check passed for oxide/plugins/PortableAirstrikes.cs against RustDedicated_Data/Managed with Oxide.References.dll and glTFast.Newtonsoft.dll excluded.
- Direct trailing-whitespace scan passed for oxide/plugins/PortableAirstrikes.cs, Docs/AirStrikes/portable_airstrikes_development_log.md, UPDATED_FILES_FOR_UPLOAD.txt, and UPDATED_FILES_FOR_UPLOAD_PORTABLE_AIRSTRIKES_WARNINGS_2026-07-08.md.

Previous upload note follows.

---

Portable Airstrikes audit webhook forwarding - 2026-07-08

What changed:
- PortableAirstrikes is now v0.1.15.
- Added `ConfigVersion=15`.
- Added disabled-by-default `AuditWebhooks` config for Discord webhook audit forwarding.
- Existing local `Audit:` console lines and `PortableAirstrikes_Data.RecentCalls` history are unchanged.
- When enabled, selected started/completed/failed/refunded/cancelled audit records can be mirrored to Discord embeds.
- Validation-failure webhook posts are off by default through `AuditWebhooks.SendValidationFailures=false`.
- Bad/missing webhook URLs print warnings only and do not block strike validation, RP/token handling, cooldowns, execution, cleanup, or local audit history.
- Existing direct/CUI validation, RP/token/cooldown/refund behavior, cancel/status, heavy warning markers, default-disabled homing/MLRS gates, loot behavior, and monument blocking behavior were left unchanged.

Rust server plugin file to upload:
- oxide/plugins/PortableAirstrikes.cs

Local source hash:
- SHA256: 0E6767A63420A5E52E80C41473CBBA50B8FBC168051F6603A8B4A76BC94F430F

Detailed Airstrikes handoff:
- UPDATED_FILES_FOR_UPLOAD_PORTABLE_AIRSTRIKES_WEBHOOKS_2026-07-08.md

Reload after upload:
- oxide.reload PortableAirstrikes

Safe default smoke:
- /strike debugping
- /strike
- /strike beancan_drop
- /strike debug history 5

Optional webhook smoke:
- Reload once so `AuditWebhooks` is written to config.
- Set `AuditWebhooks.Enabled=true` and `AuditWebhooks.DiscordWebhookUrl` to the private Discord webhook URL.
- Reload `PortableAirstrikes`, then run a cheap completed strike.
- Expected: a Discord embed appears for configured audit result types.

Local verification:
- Roslyn compile check passed for oxide/plugins/PortableAirstrikes.cs against RustDedicated_Data/Managed with Oxide.References.dll and glTFast.Newtonsoft.dll excluded.
- Direct trailing-whitespace scan passed for oxide/plugins/PortableAirstrikes.cs, Docs/AirStrikes/portable_airstrikes_development_log.md, UPDATED_FILES_FOR_UPLOAD.txt, and UPDATED_FILES_FOR_UPLOAD_PORTABLE_AIRSTRIKES_WEBHOOKS_2026-07-08.md.

Previous upload note follows.

---

Portable Airstrikes player cancel/status and heavy warning markers - 2026-07-08

What changed:
- PortableAirstrikes is now v0.1.14.
- Added `ConfigVersion=14`.
- Added player commands:
  - /strike status
  - /strike cancel
- Added pre-impact player cancellation with configured RP/token restoration and cooldown clearing.
- Added public native heavy-strike warning markers using `MapMarkerGenericRadius` while `General.UseMapMarkersForHeavyStrikes=true`.
- Added marker tuning config:
  - `General.HeavyStrikeMapMarkerSize`
  - `General.HeavyStrikeMapMarkerAlpha`
- Added cancel config:
  - `General.AllowPlayerCancelBeforeImpact`
  - `General.RefundPlayerCancelledCallsBeforeImpact`
- Existing direct/CUI validation, RP/token/cooldown/refund behavior, audit history, default-disabled homing/MLRS gates, loot behavior, and monument blocking behavior were left unchanged.

Rust server plugin file to upload:
- oxide/plugins/PortableAirstrikes.cs

Local source hash:
- SHA256: 0BECFA4B88A3ADD37DEB5BCC3C814A9CDEC0C60561DE0160E6B7FB0B110FE891

Detailed Airstrikes handoff:
- UPDATED_FILES_FOR_UPLOAD_PORTABLE_AIRSTRIKES_CANCEL_MARKERS_2026-07-08.md

Reload after upload:
- oxide.reload PortableAirstrikes

Safe default smoke:
- /strike debugping
- /strike
- /strike beancan_drop
- /strike status
- /strike debug history 5
- /strike debug active

Cancel/marker smoke:
- Start a heavy strike with a warning delay, such as /strike a10_strafe.
- Run /strike status during the warning countdown.
- Run /strike cancel before impact starts and confirm a cancelled/refunded audit row.
- Let a heavy strike complete and confirm the Rust map warning marker disappears afterward.

Local verification:
- Confirmed generic radius marker prefab exists in Bundles/AssetSceneManifest.json.
- Roslyn compile check passed for oxide/plugins/PortableAirstrikes.cs against RustDedicated_Data/Managed with Oxide.References.dll and glTFast.Newtonsoft.dll excluded.
- Direct trailing-whitespace scan passed for oxide/plugins/PortableAirstrikes.cs.

Previous upload note follows.

---

Portable Airstrikes audit logging and admin debug - 2026-07-08

What changed:
- PortableAirstrikes is now v0.1.13.
- Added `ConfigVersion=13`.
- Added `General.RecentCallHistoryLimit`, default `50`, clamped to `0-200`.
- Added bounded `RecentCalls` audit history in `PortableAirstrikes_Data`.
- Added server-console `Audit:` lines for validation failures, charge failures, accepted calls, completed calls, failed/refunded calls, and plugin-unload cancellations.
- Added admin debug commands:
  - /strike debug history [count]
  - /strike debug stats
  - /strike debug active
- Existing homing/MLRS gates, loot behavior, monument blocking behavior, RP/token/cooldown/refund flow, and strike executors were left unchanged.

Rust server plugin file to upload:
- oxide/plugins/PortableAirstrikes.cs

Local source hash:
- SHA256: F593363136EC57C1B0D7C94406699B9AFEE1508078A037B3C23B8272B3358298

Detailed Airstrikes handoff:
- UPDATED_FILES_FOR_UPLOAD_PORTABLE_AIRSTRIKES_AUDIT_2026-07-08.md

Reload after upload:
- oxide.reload PortableAirstrikes

Safe default smoke:
- /strike debugping
- /strike
- /strike beancan_drop
- /strike debug history 5
- /strike debug stats
- /strike debug active
- /strike debugping
- /strike a10_strafe

Expected:
- Existing direct/CUI strike flow still charges RP and consumes one token exactly once.
- Default-disabled homing/MLRS entries stay gated unless deliberately enabled.
- Server console prints clear `Audit:` lines for accepted/completed/failed/validation-failed calls.
- `/strike debug history [count]` shows newest bounded audit records to admins.
- `/strike debug stats` shows stored stat counters.
- `/strike debug active` shows currently active calls or reports none.

Local verification:
- Roslyn compile check passed for oxide/plugins/PortableAirstrikes.cs against RustDedicated_Data/Managed with Oxide.References.dll and glTFast.Newtonsoft.dll excluded.

Previous upload note follows.

---

Portable Airstrikes default-off homing missile executor - 2026-07-08

What changed:
- PortableAirstrikes is now v0.1.12.
- Added `ConfigVersion=12`.
- Added a default-disabled homing missile executor for `homing_heli` and `homing_jet`.
- Added executor routes:
  - `attack_heli:homing_missile`
  - `cargo_plane_jet:homing_missile`
- Homing missiles use HV rocket projectile visuals and bounded tracking using `MissileCount`, `MaxCount`, `MaxTrackingSeconds`, `MaxTrackingDistance`, `VehicleDamageScale`, and `SplashRadius`.
- Vehicle-only strikes now require a tracked target entity ID during validation, so invalid vehicle targets fail before RP/token/cooldown work.
- `homing_heli` and `homing_jet` remain disabled by default.
- Loot injection was left untouched and remains deferred until the end.

Rust server plugin file to upload:
- oxide/plugins/PortableAirstrikes.cs

Local source hash:
- SHA256: E0F58FA0614A1E1F3698156AB855A905A2111852CD116E83EEDEB9C5F780718F

Detailed Airstrikes handoff:
- UPDATED_FILES_FOR_UPLOAD_PORTABLE_AIRSTRIKES_HOMING_2026-07-08.md

Reload after upload:
- oxide.reload PortableAirstrikes

Safe default smoke:
- /strike debugping
- /strike
- /strike beancan_drop
- /strike debugping
- /strike a10_strafe

Homing opt-in smoke:
- Reload once first so `ConfigVersion=12` is saved.
- Set `StrikeDefinitions.homing_heli.Enabled=true` in `oxide/config/PortableAirstrikes.json`.
- Reload `PortableAirstrikes` again.
- Grant `portableairstrikes.use.homing.heli`, give test tokens, then aim at a live vehicle and run `/strike debugping`.
- Expected target debug: vehicle ping with an entity ID.
- Run `/strike homing_heli` in a safe open area.

Expected:
- Existing direct/CUI strike flow still charges RP and consumes one token exactly once.
- Default-disabled homing/MLRS entries do not appear unless deliberately enabled after the config save.
- Opt-in `homing_heli` launches configured HV-rocket visuals and tracks the live vehicle target within configured time/distance.
- If the target is gone before the first missile launches, RP/token/cooldowns follow the existing refund/restore path.

Local verification:
- Confirmed HV rocket prefab exists in `Bundles/AssetSceneManifest.json`.
- Roslyn compile check passed for oxide/plugins/PortableAirstrikes.cs against RustDedicated_Data/Managed with Oxide.References.dll and glTFast.Newtonsoft.dll excluded.

Previous upload note follows.

---

Portable Airstrikes opt-in monument blocking - 2026-07-08

What changed:
- PortableAirstrikes is now v0.1.11.
- Added `ConfigVersion=11`.
- Added opt-in monument blocking under `General`.
- New config keys:
  - `BlockMonuments`
  - `BlockMonumentsForHeavyStrikesOnly`
  - `MonumentBlockRadiusPadding`
  - `DefaultMonumentBlockRadius`
  - `BlockedMonumentNames`
- `BlockMonuments` defaults to false, so upload/reload does not change current strike behavior.
- When enabled, blocked monument targets fail validation before RP charge, token consume, or cooldown start.
- Added `/strike debug monument` and `/strike debug safety`.
- Loot injection was left untouched and remains deferred until the end.

Rust server plugin file to upload:
- oxide/plugins/PortableAirstrikes.cs

Local source hash:
- SHA256: C26FC8E307D3617E5EFBEBFD93808C792C67407FE33F4936C009E14570B0D6D7

Detailed Airstrikes handoff:
- UPDATED_FILES_FOR_UPLOAD_PORTABLE_AIRSTRIKES_MONUMENTS_2026-07-08.md

Reload after upload:
- oxide.reload PortableAirstrikes

Safe default smoke:
- /strike debugping
- /strike
- /strike beancan_drop
- /strike debugping
- /strike a10_strafe

Monument debug smoke:
- Aim near a configured monument and run `/strike debugping`.
- Run `/strike debug monument`.
- Expected while `General.BlockMonuments=false`: the debug command can report a matched monument zone, but strike validation remains unchanged.
- Optional enabled test: set `General.BlockMonuments=true`, reload, target a configured monument, and try a heavy strike such as `/strike a10_strafe`.
- Expected enabled test: the strike is rejected before RP/token/cooldown changes.

Expected:
- Existing direct/CUI strike flow still charges RP and consumes one token exactly once.
- Loot behavior is unchanged.
- Monument blocking remains disabled until deliberately enabled in config.
- Default-disabled homing/MLRS entries stay gated by config.

Local verification:
- Roslyn compile check passed for oxide/plugins/PortableAirstrikes.cs against RustDedicated_Data/Managed with Oxide.References.dll and glTFast.Newtonsoft.dll excluded.

Previous upload note follows.

---

Portable Airstrikes default-off MLRS executor - 2026-07-08

Review result:
- The Airstrike Authorization item is not a custom prefab; it is a normal Rust `targeting.computer` item with configured display name/skin.
- Admin grants and loot injection both create the same token through `CreateAirstrikeToken`.

What changed:
- PortableAirstrikes is now v0.1.10.
- Added `ConfigVersion=10`.
- Added a one-time old-config safety reset that disables `homing_heli`, `homing_jet`, `mini_mlrs`, and `full_mlrs` on first v0.1.10 load.
- Changed default `homing_heli`, `homing_jet`, and `mini_mlrs` definitions to disabled by default.
- Added a default-disabled MLRS executor for `mini_mlrs` and `full_mlrs`.
- MLRS uses real Rust prefab `assets/content/vehicles/mlrs/rocket_mlrs.prefab`.
- Homing missiles remain deferred.

Rust server plugin file to upload:
- oxide/plugins/PortableAirstrikes.cs

Local source hash:
- SHA256: F683681B993689BDFFD2C355B99DE542E0DB47C00553A15A22FB0B52B7A0AE18

Detailed Airstrikes handoff:
- UPDATED_FILES_FOR_UPLOAD_PORTABLE_AIRSTRIKES_MLRS_2026-07-08.md

Reload after upload:
- oxide.reload PortableAirstrikes

Safe default smoke:
- /strike debugping
- /strike
- /strike beancan_drop
- /strike debugping
- /strike a10_strafe

MLRS opt-in smoke:
- Reload once first so `ConfigVersion=10` is saved and experimental entries are disabled.
- Then set `StrikeDefinitions.mini_mlrs.Enabled=true` in `oxide/config/PortableAirstrikes.json`.
- Reload `PortableAirstrikes` again.
- Grant `portableairstrikes.use.mlrs.mini`, give test tokens, then run `/strike debugping` and `/strike mini_mlrs` in a safe open area.

Expected:
- Existing direct/CUI strike flow still charges RP and consumes one token exactly once.
- Default-disabled homing/MLRS entries do not appear unless deliberately enabled after the v0.1.10 config save.
- Opt-in `mini_mlrs` launches configured `rocket_mlrs.prefab` projectiles within configured spread.

Local verification:
- Confirmed MLRS rocket prefab exists in `Bundles/AssetSceneManifest.json`.
- Roslyn compile check passed for oxide/plugins/PortableAirstrikes.cs against RustDedicated_Data/Managed with Oxide.References.dll and glTFast.Newtonsoft.dll excluded.

Previous upload note follows.

---

Portable Airstrikes loot token injection - 2026-07-08

What changed:
- PortableAirstrikes is now v0.1.9.
- Added optional additive loot injection for the generic Airstrike Authorization item.
- Added `OnLootSpawn(LootContainer)` and `OnLootSpawn(LootFill)` support.
- Loot rules can match short prefab names, full prefab names, object names, and normalized prefab file names.
- Injection uses the same configured token shortname/display name/skin as `/strike giveitem`.
- `LootDistribution.Enabled` remains disabled by default, so no config upload is required unless loot injection should go live immediately.
- Existing CUI and direct-command strike behavior remains supported.

Rust server plugin file to upload:
- oxide/plugins/PortableAirstrikes.cs

Local source hash:
- SHA256: FEC7A71C9A056EDEBCCB6234648F7B00DB577F53EF12B24DF2FBC25464F1C145

Detailed Airstrikes handoff:
- UPDATED_FILES_FOR_UPLOAD_PORTABLE_AIRSTRIKES_2026-07-08.md

Reload after upload:
- oxide.reload PortableAirstrikes

Quick live smoke:
- Upload oxide/plugins/PortableAirstrikes.cs.
- Run oxide.reload PortableAirstrikes.
- Leave LootDistribution.Enabled=false first and confirm normal loot behavior is unchanged.
- Set LootDistribution.Enabled=true for one configured low-risk container rule if you want to test injection live.
- Spawn/find that container and confirm the Airstrike Authorization item can appear without replacing existing loot.
- Run /strike debugping, /strike, click A-10 BRRRRT Run, and confirm.

Expected:
- Loot-generated tokens match the same token validation path as admin-granted tokens.
- RP/token/cooldowns still follow the existing direct-command pipeline.
- The picker still opens on a fresh target and the confirmation panel starts the strike after CONFIRM.

Local verification:
- Roslyn compile check passed for oxide/plugins/PortableAirstrikes.cs against RustDedicated_Data/Managed with Oxide.References.dll and glTFast.Newtonsoft.dll excluded.

Previous upload note follows.

---

RaidlandsEvents ServerRewards purchase debit hotfix - 2026-07-08

What changed:
- RaidlandsEvents is now v0.1.20.
- `/raidme` is now reserved for the future player-owned base defense/bounty mode and no longer charges or starts the public CopyPaste raid-base event.
- Current public CopyPaste raid-base purchases use `/eventbuy [layoutId|random]` or `/raidbase [layoutId|random]`.
- Config list loading now replaces default list values instead of appending JSON rows onto defaults; this fixes `Purchase.Costs` loading as four rows even when the JSON file has two.
- Purchase RP balance checks now probe ServerRewards with `BasePlayer`, `player.userID`, parsed SteamID ulong, and `UserIDString`.
- Purchase costs are now aggregated before preflight/charge, so duplicate RP/item rows cannot pass independently and then fail after a partial debit.
- Purchase RP debit now stops with a clear insufficient-RP message when the live ServerRewards balance is below the configured cost.
- Purchase RP insufficient/rejected messages now include the purchaser display name and SteamID.
- Purchase RP debit now tries the same ServerRewards argument forms and stops on the first accepted debit.
- If all ServerRewards API debit calls return false despite enough RP, RaidlandsEvents falls back to ServerRewards' configured admin command path from `oxide/config/ServerRewards.json`, then tries `xp` and `rp`, and verifies the before/after balance.
- Added `revents.purchases balance [playerNameOrSteamId]` to show the exact ServerRewards balance/check details RaidlandsEvents sees.
- Added `/eventbuy balance` and `/raidbase balance` to show the same balance identity/check path used by the purchasing player.
- `revents.purchases status` now shows the effective aggregated cost and raw configured cost row count.
- Purchase/refund RP credits now also verify the before/after ServerRewards balance and fall back to the configured admin add command if API credit calls do not restore the expected balance.
- If ServerRewards still rejects the debit, the player-facing error includes the requested amount, before/after balances, balance check details, and each attempted call result.
- This targets public raid-base purchase debit/refund failures while reserving `/raidme` for the future player-base defense mode.

Rust server plugin file to upload:
- oxide/plugins/RaidlandsEvents.cs

Docs/reference files updated:
- Docs/events-manager/RaidlandsEvents_RaidBase_MVP_Implementation_Status.md
- UPDATED_FILES_FOR_UPLOAD.txt

Reload after upload:
- oxide.reload RaidlandsEvents

Quick live retry:
- revents.purchases status
- /eventbuy random

Expected:
- If you have at least 12,000 RP and 50,000 scrap, RP debit should succeed.
- If the event then starts, both RP and scrap are charged and cooldowns update.
- If start fails after charging, refund behavior follows the existing Purchase refund settings.

Local verification:
- oxide/config/RaidlandsEvents.json parses successfully.
- Roslyn compile check passed for oxide/plugins/RaidlandsEvents.cs v0.1.20 against RustDedicated_Data/Managed with Oxide.References.dll and glTFast.Newtonsoft.dll excluded.
- Remaining warnings are expected Oxide plugin-reference warnings for CopyPaste, RaidlandsSentryTurrets, and ServerRewards.
- Live Oxide reload and `/eventbuy random` or `/raidbase random` retry are still required.

Previous upload note follows.

---
Portable Airstrikes v0.1.3 live-upload reminder - 2026-07-08

The live screenshot that says only `beancan_drop`, `f1_cluster`, and `smoke_screen`
are supported is from the old v0.1.2 plugin code. Upload/reload the local v0.1.3
plugin before testing `flash_breach`, `molotov_drop`, or `he_40mm_micro`.

Rust server plugin files to upload:
- oxide/plugins/PortableAirstrikes.cs

Local source hash:
- SHA256: 8BD7C56B1B4CA65FA2A54C4FB60A57C64B2841EFF8E546D8A2D8C2E622E6BC2E

Reload after upload:
- oxide.reload PortableAirstrikes

Quick smoke:
- /strike debugping
- /strike molotov_drop

Expected v0.1.3 failure-message change if an unsupported strike is tested:
- It should list `flash_breach`, `molotov_drop`, and `he_40mm_micro` as supported.

Detailed Airstrikes handoff:
- UPDATED_FILES_FOR_UPLOAD_PORTABLE_AIRSTRIKES_2026-07-08.md

Previous upload note follows.

---

RaidlandsEvents sentry ownerless scope hotfix - 2026-07-07

What changed:
- RaidlandsSentryTurrets is now v1.0.11.
- The ownerless CopyPaste sentry fallback is no longer automatic by default.
- RaidlandsEvents-tracked sentries are still managed through exact pasted entity IDs from `OnRaidlandsTrackedPasteFinished` and active `RaidlandsEvents` saved data.
- Untracked ownerless `sentry.scientist.static` entities are repaired back to native peacekeeper behavior on plugin load by default.
- The repair path clears current targets and restores prefab range, health, and native base protection.
- Broad ownerless management commands now require an explicit `confirm` argument because native Outpost sentries are ownerless too.
- New emergency command:
  - raidlands.sentry.repairnative

Rust server plugin files to upload for this hotfix:
- oxide/plugins/RaidlandsSentryTurrets.cs

No config upload is required:
- `oxide/config/RaidlandsSentryTurrets.json` parses successfully.
- Reloading the plugin will save the new default safety keys automatically.

Reload after upload:
- oxide.reload RaidlandsSentryTurrets

Immediate live checks:
- raidlands.sentry.scan all
- If Outpost turrets are still targeting players after reload:
  - raidlands.sentry.repairnative
- For event raid bases, confirm the RaidlandsEvents log still reports `managedSentries=<count>` after paste completion.

Local verification:
- Roslyn compile check passed for `oxide/plugins/RaidlandsSentryTurrets.cs` v1.0.11 against `RustDedicated_Data/Managed`.
- `oxide/config/RaidlandsSentryTurrets.json` parses successfully.
- Live reload and in-game Outpost/event smoke are still required.

Previous upload note follows.

---

RaidlandsEvents player purchase trigger - 2026-07-07

What changed:
- RaidlandsEvents is now v0.1.8.
- Public player-purchased raid-base events are implemented but disabled by default.
- Player commands:
  - /raidme [layoutId|random]
  - /eventbuy [layoutId|random]
  - revents.purchase <layoutId|random>
- Admin purchase/refund commands:
  - revents.purchases status
  - revents.purchases refunds
  - revents.purchases retry
- New permission:
  - raidlandsevents.player.purchase
- Purchase config supports:
  - ServerRewards RP cost
  - Rust item cost
  - allowed layout list
  - random layout purchases
  - minimum online player gate
  - per-player cooldown
  - global cooldown
  - purchaser announcements
  - refund on start failure
- Active raid-base instances now retain purchase trigger, purchaser, and cost metadata.
- If CopyPaste accepts a purchase start but later reports paste failure, RaidlandsEvents refunds the cost or queues it in PendingPurchaseRefunds.
- Item refunds require the player to be online; queued refunds can be retried with revents.purchases retry.
- Purchase announcements explicitly keep the event public/counterable and do not reserve loot or rewards for the purchaser.

Rust server plugin/config files to upload for this latest update:
- oxide/plugins/RaidlandsEvents.cs
- oxide/config/RaidlandsEvents.json

Docs/reference files updated:
- Docs/events-manager/RaidlandsEvents_RaidBase_MVP_Implementation_Status.md
- UPDATED_FILES_FOR_UPLOAD.txt

Reload after upload:
- Prefer stopping/cleaning active events first, because local config still has `Cleanup On Unload=true`.
- oxide.reload RaidlandsEvents

Admin/status smoke:
- Run `oxide.reload RaidlandsEvents`.
- Run `revents.status`.
- Expected: status includes `purchases=off` and `pendingPurchaseRefunds=0`.
- Run `revents.purchases status`.
- Expected: purchases are disabled, permission is `raidlandsevents.player.purchase`, default layout is `random`, and default cost is `12,000 RP + 50,000 scrap`.

Player purchase smoke:
- Keep at least one valid layout enabled, for example:
  - revents.layouts scan
  - revents.layouts enable justintoactive-1
- Grant a test player:
  - oxide.grant user <steamId> raidlandsevents.player.purchase
- Intentionally set `Purchase.Enabled=true` in `oxide/config/RaidlandsEvents.json`.
- Reload:
  - oxide.reload RaidlandsEvents
- Have the test player run:
  - /raidme random

Expected purchase behavior:
- If the player lacks RP or scrap, the command explains the missing cost and charges nothing.
- If the event is accepted, the player is charged the configured RP/item cost.
- The event starts at a random validated public location.
- Chat announces the public raid base as counterable and not reserved.
- `revents.purchases status` shows updated global cooldown.
- A second purchase during cooldown is rejected without charging.

Failure/refund smoke:
- To test refunds safely, temporarily create a blocked start condition after charge preflight, or use a staging CopyPaste failure.
- Expected with `Refund On Start Failure=true`:
  - Immediate start failure refunds the purchase cost.
  - Async paste failure refunds the purchase cost if possible.
  - If refund cannot complete, `revents.purchases refunds` lists the pending refund.
  - `revents.purchases retry` pays queued refunds after ServerRewards is available and/or the player is online for item return.

Local verification:
- oxide/config/RaidlandsEvents.json parses successfully.
- Roslyn compile check passed for oxide/plugins/RaidlandsEvents.cs v0.1.8 against RustDedicated_Data/Managed with Oxide.References.dll and glTFast.Newtonsoft.dll excluded.
- Remaining warnings are expected Oxide plugin-reference warnings for CopyPaste, RaidlandsSentryTurrets, and ServerRewards.
- Live Oxide reload and in-game smoke are still required to prove runtime behavior.
